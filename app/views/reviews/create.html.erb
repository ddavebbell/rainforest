<h2>reviews/new</h2>

<div class="create_reviews">
	<p>Write a review:</p>
	<%= form_for [@product, @review] do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name %><br>

		<%= f.label :text %>
		<%= f.text_field :text %><br>

		<%= f.submit %>
	<% end %>
</div>
